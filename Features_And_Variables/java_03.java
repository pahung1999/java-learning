public class Demo3{
    public static void main(String[] args) {
        // Note: The method should start with a lowercase letter, and the next word should be capitalized.
        // For example:
        // run()
        // noFast()
        // getExample()


        

    // Quy tắc viết hoa viết thường:
    //     Tên biến nội bộ sẽ bắt đầu bằng chữ thường, và các từ trong tên biến sẽ bắt đầu bằng chữ hoa (CamelCase).
    //     Tên biến không nên bắt đầu bằng dấu gạch dưới (_) hoặc ký hiệu đô la ($).

    // Ý nghĩa của tên biến:
    //     Tên biến cần ngắn gọn nhưng có ý nghĩa, giúp người đọc dễ hiểu nhiệm vụ của biến đó. Tên biến nên dễ nhớ và phản ánh rõ mục đích của nó.

    // Tránh tên biến ngắn:
    //     Các biến chỉ có một ký tự (như i, j, k) nên được sử dụng cẩn thận và chỉ trong những trường hợp cụ thể, ví dụ như trong vòng lặp. Nên tránh sử dụng cho các biến quan trọng khác.

    // Tên biến tạm thời:
    //     Có những tên biến phổ biến cho các biến tạm thời, như width, height, v.v.

    // Ví dụ:
    // int             i;
    // char            c;
    // float           myWidth;



    

    // Cách đặt tên:
    //     Tên của các hằng số (constants) nên được viết bằng chữ hoa hoàn toàn và các từ nên được phân tách bằng dấu gạch dưới (_).
    //     Ví dụ: MAX_WIDTH hay MIN_WIDTH.

    // Tránh việc sử dụng hằng số kiểu ANSI:
    //     Hằng số được định nghĩa theo chuẩn ANSI (American National Standards Institute) nên được tránh nếu không cần thiết, nhằm đảm bảo sự rõ ràng và dễ dàng trong việc gỡ lỗi (debugging).

    // Ví dụ về hằng số:
    //     static final int MIN_WIDTH = 4;
    //     static final int MAX_WIDTH = 999;
    //     static final int SIZE_OF_THE_CPU = 1;

    }
}

